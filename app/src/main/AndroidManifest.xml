<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.s215087038.wefixx">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".rsa.RSA">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".student.Student" />
        <!-- <activity android:name=".Login" /> -->
        <activity android:name=".manager.Manager" />
        <activity android:name=".rsa.Manage" />
        <activity android:name=".manager.Reports" />
        <!-- <activity android:name=".Main2Activity" /> -->
        <activity android:name=".manager.Delayed" />
        <activity android:name=".History" />
        <activity android:name=".Hist" />
        <!--<activity android:name=".rsa.RSA" />-->
        <activity android:name=".student.StudentHistory" />
        <activity android:name=".FAQ" />
        <activity android:name=".NewRequest"></activity>
    </application>

</manifest>